<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-TH1PNWMT83"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());

      gtag('config', 'G-TH1PNWMT83');
    </script>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="/styles/bundle.css">
    <link rel="icon" href="/img/favicon.ico">
    <link rel="me" href="https://peoplemaking.games/@gabtoschi">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;0,1000;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900;1,1000&family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <title>{{ metatitle | default: title | default: default-meta.title }}</title>

    <meta property="og:title" content="{{ metatitle | default: title | default: default-meta.title }}">
    <meta property="og:type" content="article" />
    <meta property="og:image" content="{{ thumbnail | default: default-meta.thumbnail | prepend: default-meta.baseUrl  }}">
    <meta property="og:url" content="{{ permalink | prepend: default-meta.baseUrl }}">
    <meta property="og:description" content="{{ description | default: default-meta.description }}">
    <meta property="og:site_name" content="Gabriel Toschi">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:image:alt" content="{{ metatitle | default: title | default: default-meta.title }}">
  </head>

  <body>
    {% render "components/header", lang: lang, translate: translate %}

    <div>
      {{ content }}
    </div>

    {% render "components/footer", lang: lang %}
  </body>
</html>